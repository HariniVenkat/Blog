<h1>Listing articles</h1>
 
<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
  </tr>
 
  <% @articles.each do |article| %>  <!-- this has been fetched from index method of article controller-->
    <tr>
      <td><%= article.title %></td>
      <td><%= article.text %></td>
 
<!--see parameter is been passed....it will have access to only this article...not all -->
             <td><%= link_to 'Show', article_path(article) %></td>
 
<!--goes to edit action....see parameter...... alternate1.1 for alternative..-->
      <td><%= link_to 'Edit', edit_article_path(article) %></td>


<!--DELETE /articles/:id(.:format)      articles#destroy...this is how delete works.....<a href='http://example.com/articles/1/destroy'>look at this cat!</a>-->
<td><%= link_to 'Destroy', article_path(article),
              method: :delete,
              data: { confirm: 'Are you sure?' } %></td>

    </tr>




    </tr>
  <% end %>
</table>




<%= link_to 'New article', new_article_path %>